{
  "name": "iceberg-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack iceberg tracker — Antarctic and North Atlantic iceberg locations, sizes, and drift analytics.",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "seed": "pnpm --filter api seed"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  }
}
