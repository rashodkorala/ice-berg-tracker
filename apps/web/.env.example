# Origin of the FastAPI backend. Used both at build time and at runtime on the
# server; the browser always talks to /api/* via Next's rewrite proxy, so CORS
# is never an issue in the browser.
API_ORIGIN=http://127.0.0.1:8000
